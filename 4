#字符串内置方法
new_str="  welcome to xf1433.com.xf1433"
print(new_str.find("xf")) #找所要寻找的字符串的位置
print(new_str.find("xf",13))  #从13位开始，找所要寻找的字符串的位置
print(new_str.count("com"))  #查找字符串出现了几次
print(new_str.replace("xf","xiaofeng",1))  #加1代表只替换一个，不写默认全部替换
print(new_str.split(".",1))  #加1代表只切割一次，不写默认全切
print(new_str.startswith("welcome"))  #判断是否以该字符串开头
print(new_str.upper())  #全部转换为大写，小写为lower
print(new_str.strip())  #去掉开头空格